

enum EValveTurnInteractionAnimationDirection
{
	Unset,
	Valid,
	Invalid
};

enum EValveTurnInteractionAnimationType
{
	LeftStruggle,
	LeftEnd,
	Left,
	None,
	Right,
	RightEnd,
	RightStruggle,
};

namespace ValveTurnTags
{
	const FName InteractionActor = n"InteractionActor";

}